#pragma once
#include <vector>

template <typename T>
bool compareVectors(std::vector<T> first, std::vector<T> second);

template <typename T>
void printVector(std::vector<T> toPrint);

// ------------------------
#include "Helpers.inl"